// years of experince

// المتغير
const a = 5;

document.addEventListener("DOMContentLoaded", () => {
    const element = document.getElementById("years of experince");
    if (element) {
        element.textContent = a; 
    }
});


// technical skills

// المتغير
const b = 15;

document.addEventListener("DOMContentLoaded", () => {
    const element = document.getElementById("technical skills");
    if (element) {
        element.textContent = b; 
    }
});


// satsfied clients

// المتغير
const c = 120;

document.addEventListener("DOMContentLoaded", () => {
    const element = document.getElementById("satsfied clients");
    if (element) {
        element.textContent = c; 
    }
});


// projects completed

// المتغير
const d = 80;

document.addEventListener("DOMContentLoaded", () => {
    const element = document.getElementById("projects completed");
    if (element) {
        element.textContent = d; 
    }
});


